#include <math.h>
#include <stdio.h>
/*-sqrt()- Raiz Quadrada
-pow() - potência
-abs()/fabs() - valor absoluto
-sin(),cos(),tan() - trigonometria*/

int main(){
    int base = 2, expoente = 3;
    printf("2 elavado a 3: %.2f\n", pow(base ,  expoente));

    printf("Raiz quadrade de 25: %.2f\n", sqrt(25));
    return 0;
}